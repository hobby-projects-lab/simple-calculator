package hpl.apps.android.math.ui.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.material3.Shapes

val shapes = Shapes()

data object CalculatorShapes {
    val round = RoundedCornerShape(50)
}